{
  "name": "it-batch",
  "version": "0.0.1",
  "description": "Takes an async iterator that emits things and emits them as fixed size batches",
  "main": "index.js",
  "scripts": {
    "test": "nyc --check-coverage --lines 100 --reporter html --reporter lcov ava",
    "lint": "standard",
    "coveralls": "npm test && cat ./coverage/lcov.info | coveralls"
  },
  "author": "Alex Potsides <alex@achingbrain.net>",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/achingbrain/it-batch.git"
  },
  "bugs": {
    "url": "https://github.com/achingbrain/it-batch/issues"
  },
  "homepage": "https://github.com/achingbrain/it-batch#readme",
  "devDependencies": {
    "ava": "^1.4.1",
    "coveralls": "^3.0.2",
    "it-all": "^1.0.0",
    "nyc": "^14.0.0",
    "standard": "^12.0.1"
  }
}
